# 6. Create and validate an execution environment with the AI assistant
## 6.1. Prerequisites




- The GitHub Copilot Extension installed and active.
- Ansible VS Code extension is installed.
- The Ansible MCP server is enabled. Ensure the setting `    ansible.mcpServer.enabled` is set to `    true` .
- Ansible development tools is installed either through the AI assistant ( `    adt_check_env` ) or manually.


